# 修改清单（v1，2026-09-04）

基线：MUnique/OpenMU master、sven-n/MuMain main（均为 2026-09-04 快照）。
约定：`client/` = MuMain，`server/` = OpenMU。

## 新增文件

| 文件 | 说明 |
|---|---|
| `client/src/Localization/Game.zh-CN.resx` | 游戏 UI 简体中文，3227 条（zh-TW 转换 + 术语校正 + 3 个新键） |
| `client/src/Localization/Metadata.zh-CN.resx` | 物品/属性元数据简体中文，59 条 |
| `client/src/Localization/Editor.zh-CN.resx` | 内置编辑器简体中文，142 条 |
| `client/src/bin/fonts/NotoSansSC-Regular.otf` | 思源黑体（OFL），中文字形支持 |
| `client/src/bin/fonts/NotoSansSC-Bold.otf` | 思源黑体粗体 |
| `server/src/GameLogic/Properties/PlayerMessage.zh-CN.resx` | 游戏内系统消息中文，196/196 |
| `server/src/Web/AdminPanel/Properties/Resources.zh-CN.resx` | GM 管理面板界面中文，289/289 |
| `server/src/Web/Shared/Properties/Resources.zh-CN.resx` | 共享组件中文，65/65 |
| `server/src/Web/Map/Properties/Resources.zh-CN.resx` | 实时地图页中文，21/21 |
| `server/src/DataModel/Properties/Resources.zh-CN.resx` | 系统配置说明中文，19/19 |

## 修改文件

### client/src/source/Data/GameConfig/GameConfigConstants.h
- `CfgKeys` 新增 `CfgKeyFpsPreset`（[Render] 段，键名 `FpsPreset`）
- `CfgDefaults` 新增 `CfgDefaultFpsPreset = 0`（0=显示器上限）

### client/src/source/Data/GameConfig/GameConfig.h / GameConfig.cpp
- 新增 `m_fpsPreset` 成员、`GetFpsPreset()/SetFpsPreset(int)`
- `Load()`/`Save()` 读写 `FpsPreset`

### client/src/source/App/Platform/Windows/Winmain.h
- 声明 `MuSetFpsPreset(int)`、`MuReapplyFpsPreset()`

### client/src/source/App/Platform/Windows/Winmain.cpp
- `ApplyVSyncPreferenceNow`：VSync 生效后再按 `FpsPreset` 覆盖目标帧率（>0 固定上限，-1 无上限，0 保持原逻辑）
- 新增匿名域 `g_hasPendingFpsPreset/g_pendingFpsPreset`、`ApplyFpsPresetNow`、`ApplyPendingFpsPreset`（0→`GetFPSLimit()`，-1→`SetTargetFps(-1)`，正数→固定上限）
- 新增 `MuSetFpsPreset`（持久化+挂起）、`MuReapplyFpsPreset`
- 主循环帧首：`ApplyPendingVSyncPreference()` 后新增 `ApplyPendingFpsPreset()`
- `MuApplyWindowResolution` 尾部：`MuReapplyVSyncPreference()` 后新增 `MuReapplyFpsPreset()`（切分辨率/显示器后按新刷新率重挂）

### client/src/source/UI/NewUI/Options/NewUIOptionWindow.h
- 新增成员：`m_iFpsIndex`、`m_FpsCombo`、`m_fpsLabels`、`m_fpsLabelPointers`
- 新增方法：`ApplyFps()`、`FindCurrentFpsIndex()`、`InitFpsCombo()`

### client/src/source/UI/NewUI/Options/NewUIOptionWindow.cpp
- 顶部 extern：`int GetFPSLimit();`、`void MuSetFpsPreset(int);`
- `s_Languages[]` 新增 `zh-CN`（简体中文）
- 常量区：新增 `FPS_LABEL/COMBO_*` 布局常量与预设表 `s_FpsPresets = {30,60,90,120,144,180,0,-1}`（0=显示器上限、-1=无限制）
- `Create()` 新增 `InitFpsCombo()`；`SetPos` 同步 FPS 下拉位置；`OpenningProcess` 重建 FPS 下拉（刷新 Hz 标签）；`ClosingProcess` 关闭
- `InitFpsCombo()`：生成标签（固定档位 "N FPS"；显示器上限动态带当前 Hz；无限制取 `I18N::Game::Unlimited`）并 `Setup`
- `UpdateMouseEvent`：`ComboSlot` 表新增 FPS 槽；窗口命中区 190x419 → 190x458
- `HandleCheckboxInputs`：窗口模式复选框行 356 → 395
- `RenderFrame`：SLAT_COUNT 30 → 34（边框加高 40px）
- `RenderContents`：分辨率行后插入"帧率"行（标签 `I18N::Game::FrameRate`，y=361）；窗口模式行 361 → 400
- `RenderButtons`：窗口模式勾选 356 → 395；渲染数组加入 `m_FpsCombo`
- `SetButtonInfo`：关闭按钮 y 388 → 427
- 新增 `FindCurrentFpsIndex()`（未知值回落到"显示器上限"）、`ApplyFps()`（去抖 + `MuSetFpsPreset`）

### client/src/Localization/Game.en.resx / Game.zh-TW.resx
- 各追加 3 键：`Frame rate`、`Monitor maximum`、`Unlimited`（zh-TW：幀率/顯示器上限/無限制）

### client/tools/ResxGen/CppEmitter.cs
- `KnownLanguageDisplayNames` 新增 `["zh-CN"] = "简体中文"`

### client/src/bin/config.ini.template
- `[Render]` 段新增 `FpsPreset=0` 及注释

## 未改动但相关（供执行 AI 参考）
- 客户端帧率底层：`Scenes/SceneManager.cpp`（`SetTargetFps`）、`Core/Utilities/Log/muConsoleDebug.cpp`（`$fps` 命令）
- 翻译系统：`docs/translation-system.md`；生成器 `tools/ResxGen`
- 服务端数值入口：管理面板 Configuration 各页（`docs/改造计划书.md` 第 7 章）
