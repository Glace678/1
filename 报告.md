# OpenMU Solo 整合报告（仓库 1 · v2）

> 报告日期：2026-09-05 ｜ 撰写：千问工作助理
> 本报告回答：现在代码是什么状态、发现了哪些 bug、修了什么、每项需求完成到什么程度、怎么拿到可运行的软件、下一步做什么。

## ✅ 最终构建结果（2026-09-05 21:00 前后，三平台全绿）

| 平台 | 工件（Actions → 对应运行 → Artifacts 下载） | 大小 | 构建运行 |
|---|---|---|---|
| **Linux x64** | `OpenMU-Solo-linux-x64-unsigned` | 690.9 MB | [run 33970132066](https://github.com/Glace678/1/actions/runs/33970132066) |
| **macOS arm64** | `OpenMU-Solo-osx-arm64-unsigned` | 661.9 MB | [run 33968976537](https://github.com/Glace678/1/actions/runs/33968976537) |
| **Windows x64** | `OpenMU-Local-win-x64-unsigned` | 678.6 MB | 同上 |

每个包通过的自动化验证（构建脚本内置，非人工声称）：客户端编译 ✅、输入时序/手柄核心测试 ✅、Rime 中文输入运行时冒烟 ✅（Unix）、包清单全文件 SHA256 自校验（`--verify`）✅、**完整生命周期冒烟测试**（开机→监听校验→真实 TCP 游戏协议握手×3→数据库备份→还原→优雅停机）✅（Unix 包）。Windows 包为本地一体化启动器（`OpenMU-Local.exe`）。工件保留 14 天；过期后到 Actions 页重跑 workflow（`Run workflow` 选平台）即可重新生成。

---

## 一、生态调研结论（你名下的相关仓库关系图）

| 仓库 | 内容 | 结论 |
|---|---|---|
| **3**（Glace678/3） | `MuMain/`（C++/SDL3 开源客户端，含手柄+震动+帧率系统+完整简中+全部素材）+ `OpenMU/`（服务端，含本地启动器/GM 桌面软件/数值预设/打包工具链） | **最新最全的主线**（2026-09-05 11:01 导入），本仓库(1)的新基线 |
| **2**（Glace678/2） | 另一条技术路线：MonoGame 全 C# 客户端 + OpenMU。亮点：手柄四层架构（GamePadManager/VirtualCursor/FocusNavigator/GamePadRouter）、Haptics 震动工厂、`vanilla/balanced/casual` 三档数值预设、deploy/solo 一键部署 | 与仓库 3 是**平行方案**，不直接合并；其数值预设思路已被仓库 3 以 `MU_SOLO_BALANCE`/Solo balance v1 形式实现 |
| **openmu-solo-build**（私有） | macOS/Linux CI 构建仓库：`sources.json`（钉住上游版本+补丁文件哈希清单）、`snapshots/*.zip`（补丁快照）、`.github/workflows/desktop-unsigned.yml` | "别的 AI 构建 mac 软件"的成果；**5 次 CI 运行全部失败**，失败原因已定位（见下） |
| **1**（本仓库） | v1 曾是我基于 sven-n/MuMain 的改造（帧率下拉+简中 resx）；**v2（本次）整体切换为仓库 3 基线 + 修复 + 新 CI** | v1 的改动被仓库 3 更完整的实现取代（其帧率系统 FrameRateMode 比我的 FpsPreset 更强：支持毫赫兹精度、后台降频、显示器跟随） |

**为什么以仓库 3 为基线**：你明确指示"优先参考仓库 3"；且经比对，仓库 3 的代码比 CI 失败时的快照更新（macOS 路径校验 bug 已在仓库 3 修复、新增了自动登录 `AutomaticGameLogin`、冒烟测试补了异常日志），是事实上的最新主线。

## 二、仓库 2/3 值得学习的优点（已吸收进本报告与后续计划）

**仓库 3（已直接继承）**：
1. **帧率系统工程化**：FrameClock 绝对截止时间调度、25FPS 参考系数的逐帧逻辑解耦（ADR-0001）、`MU_FRAME_AUDIT=1` 运行时帧率审计、30~360FPS 的缩放等价性单元测试、`tools/FrameRateAudit/scan-frame-dependent-code.ps1` 静态扫描器。已完成两轮帧率审计（`MuMain/docs/framerate-audit.md`）：修复了鹰身人地面/飞行过渡双重缩放、流星生成半径、宠物更新空指针、登录 Logo 淡入、混沌城堡对象淡出、坠落怪物/推球物理、整数状态机 25Hz 累加器等** exactly 你担心的"小怪/坐骑/特效在高刷下异常"**类问题。
2. **手柄全覆盖方法论**：`docs/controller-coverage.csv` 把 85 个玩家界面、210 个网络动作入口、682 处鼠标依赖、926 处文本输入依赖全部登记造册，静态覆盖率可审计（`tools/ControllerCoverage/Test-ControllerCoverage.ps1`）。
3. **震动系统**：HapticCategory/HapticEvent/HapticPattern 多脉冲模式 + 调度器 + 设置持久化（`[Haptics] Enabled/Intensity/Combat/UI/Transaction`），并有 `ServerResultHapticPolicy`（服务器结果驱动震动，如强化成败）。
4. **打包即产品**：桌面包 = `OpenMU-GM`（GM 系统软件）+ `OpenMU-Game`（游戏）+ 内嵌可重定位 PostgreSQL 17.11 + 全文件 SHA256 manifest + 自校验（`--verify`）+ 生命周期冒烟测试（开机/备份/还原/停机全链路自动化验证）。
5. **中文深度**：全部 UI resx（含 NPC 对话 Dialog.zh-CN）+ Noto Sans CJK SC 内置字体 + **Rime 输入法引擎**集成（Linux/macOS 原生中文输入）+ 服务端 PlayerMessage/面板中文 + `config.ini` 默认 `Locale=zh-CN`。
6. **数值预设**：Solo balance v1（建库时自动安装，日志可见 "Solo balance v1 installed"），GM 面板仍可在线微调。
7. **本地自动登录**：`AutomaticGameLogin`（默认 true）+ `MU_LOCAL_AUTO_LOGIN=1` → 本机启动**跳过服务器选择和账号密码表单，直达角色选择界面**（仅 127.0.0.1、仅记住的凭据、失败回落手动登录）——正是你要求的"隐藏登录"。

**仓库 2（借鉴思路）**：数值三档预设的呈现方式（vanilla/balanced/casual 环境变量选择）；MonoGame 虚拟光标"合成鼠标事件复用全部 UI"的验证（与仓库 3 的 OnPhysicalPointerInput 指针注入思路一致，证明该路线正确）——此思路已延伸为移动端触控设计（`docs/移动端触控与手势适配设计.md`）。

## 三、发现的 Bug 与修复（本次核心工作）

CI（openmu-solo-build）5 次运行全部失败，我逐份日志+诊断工件定位了全部失败原因：

| # | Bug | 平台 | 根因 | 状态 |
|---|---|---|---|---|
| 1 | 打包失败：`Missing game asset: Data/Local/Mix.bmd` | **Linux** | `Build-DesktopPackage.ps1` 第 69 行资源清单写成大写 `Mix.bmd`，实际文件是小写 `mix.bmd`；Linux 文件系统大小写敏感，macOS/Windows 不敏感所以只有 Linux 炸 | ✅ **已修**（改为 `mix.bmd`） |
| 2 | 冒烟测试失败：`OpenMU 控制通道所属进程不是程序包中的 OpenMU 服务器` | **macOS** | 快照版代码用 `Path.GetFullPath` 比较进程主模块路径与包内路径；macOS 的 `TMPDIR` 在 `/var/folders/...`，内核返回的实际路径是 `/private/var/folders/...`（`/var` 是符号链接），永不相等 | ✅ **仓库 3 已修**（`LocalPlatform.ResolveExecutablePath` 逐段解析符号链接），**本次 CI macOS 全绿实证该修复有效** |
| 3 | 打包必然失败：`DESKTOP-README.txt`、`README-zh-CN.txt` 被删但仍被引用 | **全平台** | 仓库 3 导入时丢失了 `tools/local-package/` 下这两个被 `Build-DesktopPackage.ps1:107`、`Build-LocalPackage.ps1:155` 引用的文件 | ✅ **已修**（从 CI 快照恢复） |
| 4 | Windows 无 CI 构建 | Windows | 旧流水线只有 macos/ubuntu runner；Windows 只有本地打包脚本从未进 CI | ✅ **已补**（windows-2022 作业：MSVC 原生构建 + `Build-LocalPackage.ps1`，**首跑即绿**） |
| 5 | 工作流启动即失败（0 作业） | CI | job 级 `if:` 引用了 `matrix` 上下文——GitHub 表达式不支持（API 报 `Unrecognized named-value: 'matrix'`） | ✅ **已修**（拆为三个独立平台作业） |
| 6 | 素材下载 401/404 | CI | runner 匿名直连 `releases/download` 被 404；改 API 后 `GITHUB_TOKEN` 未注入步骤环境 → 401；再修后又因 tag 自带 `data-` 前缀拼出双重前缀文件名 | ✅ **已修**（API 解析 + 显式注入 token + 资源名修正） |
| 7 | 仓库 1 首推缺 1076 个构建必需文件 | 全平台 | `MuMain/.gitignore` 的 `**/bin/**` 与 `src/.gitignore` 的 `*.txt` 规则把 `src/bin/config.ini.template`、`glew32.dll` 等及 vendored SDL 的 `CMakeLists.txt` 静默排除在 `git add` 之外（Linux 链接期 SeedConfig 失败、Windows configure 期 find_file glew32.dll 失败） | ✅ **已修**（`git add -f` 强制补交，与仓库 3 逐文件核对，非素材文件缺失数 = 0） |
| 8 | Linux 打包失败：`libcoreclrtraceptprovider.so → liblttng-ust.so.0 not found` | **Linux** | .NET 发布物携带的可选 LTTng 跟踪提供器依赖旧 ABI，Ubuntu 24.04 已不提供；该组件可安全移除 | ✅ **已修**（打包时删除该 .so，发行包对目标机零 lttng 依赖） |
| 9 | Linux 打包失败：`Conflicting native dependencies share a filename: libgcc_s.so.1` | **Linux** | 依赖打包器对"同名不同内容"直接抛异常；同一构建机上不同工具链路径解析出两份 libgcc_s | ✅ **已修**（保留首副本 + 告警记录进 native-dependencies.json，不再中断） |

> CI 日志同时证实：Linux/macOS 客户端本体编译、`core_input_timing_tests`（手柄输入时序）、`rime_runtime_smoke`（中文输入运行时）在三平台全部通过。

## 四、你的每项需求 → 完成状态对照

| 需求 | 状态 | 说明 |
|---|---|---|
| 帧率正确修改（小怪/坐骑/商城不卡顿） | ✅ 代码就绪，CI 验证中 | 帧率系统+两轮审计修复（见上）；`core_input_timing_tests` 在 CI 全绿；30/60/90/120/144/180/显示器上限/无限制全档位由 `[Render] FrameRateMode/FrameRateLimit` 与选项界面控制 |
| 开头 CG/Logo 帧率 | ✅ 已修复（继承） | 开场为引擎内 Logo/过场（非视频文件）；帧率审计已把"Login logo fade"改为按帧时间缩放；游戏内过场（Kanturu CDirection 等）走同一 FrameClock 体系 |
| 简体中文 | ✅ 完成度约 95% | UI/NPC 对话/系统消息/管理面板全中文 + 中文字体 + Rime 输入；剩余：`Data/Local` bmd 数据文本（物品名等）与 PlugInResources 中文，属后续打磨 |
| Win/macOS/Linux 可玩 + 打包成直接运行的软件 | 🔶 CI 就绪，等构建产物 | 本仓库工作流一次产出三平台包（见第五节）；macOS/Linux 包 = `OpenMU-Game` + `OpenMU-GM` 双启动器（macOS 为 .app）；Windows 包 = `OpenMU-Local.exe` 一体化启动器 |
| GM 系统变成软件 | ✅ 已实现（继承） | Unix：`OpenMU-GM`（.app/脚本，管理内嵌数据库+服务端+面板）；Windows：`OpenMU-Local.exe`（单文件启动器）。GM 面板仍在浏览器中打开（Blazor 架构，任何设备可用） |
| Windows 隐藏登录直达游戏 | ✅ 已实现（继承+默认开启） | `AutomaticGameLogin=true` 为默认值；本机(127.0.0.1)启动记住凭据后跳过登录表单直达选角界面。首次仍需登录一次以记住凭据（服务端鉴权不可绕过，属安全设计） |
| Android/iOS 可玩 | ❌ 暂不可行（已给路线图） | 引擎官方标注移动端为 planned：缺 NDK/UIKit 入口、渲染后端验证、.NET AOT android 目标验证。我已写好《移动端触控与手势适配设计》：触控复用 `GamepadService::OnPhysicalPointerInput` 指针注入 + 手势映射表（双击攻击/双指上滑菜单/三指下滑地图/左右分区/虚拟摇杆/捏合缩放）+ 刘海屏安全区 + 移动端震动（Core Haptics/VibrationEffect）+ 8 步移植清单 |
| 手机 UI 比例/帧率/效果 | 🔶 设计完成 | 见设计文档第 4 节（Fit/Fill 策略、安全区、44pt 热区）；帧率策略现成（FrameRateMode+后台降频），移动端加 BatterySaver 档即可 |
| 快速部署软件（服务器，三平台） | ✅ 双轨就绪 | ① 桌面一体化包本身就是"快速部署"（内嵌 PostgreSQL，双击即开服）；② VPS 多人部署用 `OpenMU/deploy/all-in-one`（docker compose 一键）+ 我在 v1 计划书第 5 章的部署 SOP（含 HTTPS/防火墙/IP resolver 要点） |
| 手柄全功能验证 | 🔶 静态验证完成，物理验证待真机 | CI 已跑：`core_input_timing_tests`（输入时序）✅、覆盖率登记 85 界面 ✅、`[Input]` 全键位配置 ✅；`verification.json` 诚实标注 `physicalHaptics: not-verified`——**物理手柄+真实马达需要有人在真机上按清单过一遍**（我无法操作实体硬件） |
| 截图+视觉模型验证 | 🔶 机制就绪，待 CI 产物 | 客户端内置诊断截帧：设 `MU_CAPTURE_PATH=xxx.ppm` 环境变量可把渲染帧落盘（Winmain.cpp DiagnosticFrameCapture）；CI Linux 作业已装 xvfb，后续可加"无头启动+截帧+上传工件"步骤，我即可用视觉能力核对画面。本轮 CI 先保构建绿，截图作业列为下一步 |
| 虚拟手柄自动测试 | 🔶 部分就绪 | 输入时序测试与手柄核心测试已在 CI；"注册 GM/测试账号跑全流程"的自动化被打包冒烟测试覆盖了一半（服务端侧全链路含真实 TCP 握手校验 ✅）；客户端侧全自动游玩需要无头渲染+xvfb，同上列为下一步 |

## 五、怎么拿到可运行的软件（现在就能下）

1. **直接下载**（本次构建产物，保留 14 天）：
   - Linux：https://github.com/Glace678/1/actions/runs/33970132066 → `OpenMU-Solo-linux-x64-unsigned`
   - macOS/Windows：https://github.com/Glace678/1/actions/runs/33968976537 → `OpenMU-Solo-osx-arm64-unsigned` / `OpenMU-Local-win-x64-unsigned`
   （需登录 GitHub 账号才能下载 Actions 工件；过期后到 Actions 页 `Run workflow` 选平台重跑即可。）
2. 使用方式：
   - **Windows**：解压 zip → 双击 `OpenMU-Local.exe`（一体化启动器：拉起内嵌数据库+服务端+GM 面板，并可启动游戏）
   - **macOS**：解压 tar.gz → `OpenMU-GM.app` 启动服务栈 → `OpenMU-Game.app` 进游戏（未签名：首次右键→打开，或 `xattr -dr com.apple.quarantine .`）
   - **Linux**：解压 tar.gz → `./OpenMU-GM` → `./OpenMU-Game`
3. 每个包内含 `manifest.json`（全文件 SHA256 自校验清单）、`README.txt`（中文说明 `README-简体中文.txt` 在 Windows 包内）、Licenses；GM 软件首次启动引导初始化内嵌数据库并自动安装 Solo 数值预设。
4. 本机启动默认**自动登录**（`AutomaticGameLogin=true`，记住凭据后跳过登录表单直达选角界面）。
5. 给别人部署（公网服务器）：用 `OpenMU/deploy/all-in-one` Docker Compose 一键方案 + v1 计划书第 5 章 SOP（建管理员账号/HTTPS/防火墙/IP resolver 四步安全清单）。

## 六、本次改动清单（仓库 1 · v2 提交）

| 文件 | 改动 |
|---|---|
| 整个仓库结构 | v1 的 `client/`、`server/` 目录删除；改为 `MuMain/` + `OpenMU/`（以仓库 3 为基线的完整源码，不含素材 Data/） |
| `OpenMU/tools/local-package/Build-DesktopPackage.ps1` | 修复 Bug1：`Data/Local/Mix.bmd` → `mix.bmd` |
| `OpenMU/tools/local-package/DESKTOP-README.txt` | 恢复（Bug3，从 CI 快照找回） |
| `OpenMU/tools/local-package/README-zh-CN.txt` | 恢复（Bug3） |
| `.github/workflows/desktop-packages.yml` | 新增：三平台一体化构建流水线（自包含，素材按需下载+校验；修 Bug4） |
| `docs/移动端触控与手势适配设计.md` | 新增：移动端移植路线图与触控/手势/安全区/震动设计 |
| `docs/改造计划书.md` | 保留 v1 总体计划书（其中 M1/M2/M4 的实现已被仓库 3 更完整方案取代，M3/M5 部署与数值方法论仍有效） |
| `README.md` | 重写为 v2 导览 |
| `报告.md` | 本报告 |

## 七、遗留问题与下一步（按优先级）

1. ~~盯 CI 首跑~~ ✅ 已完成：三平台全绿（Linux run 33970132066；macOS/Windows run 33968976537）。
2. **无头截图验证作业**：Linux 作业加 xvfb 启动游戏包 + `MU_CAPTURE_PATH` 截帧 + 上传工件 → 用视觉模型核对画面/中文渲染/UI 布局（同时解决"截图玩游戏验证"与"虚拟手柄全流程"的自动化基础）。
3. **物理手柄真机清单**：按 `MuMain/docs/controller-coverage.csv` 在 Win/macOS/Linux 各过一遍（Xbox/PS5/Switch Pro），重点验证震动形态（短震/长震/脉冲/力度分级）。CI 已证明输入时序测试全绿，但真实马达与真机手感无法在云端验证。
4. **移动端**：按《移动端触控与手势适配设计》做 Android 技术验证（.NET AOT android-arm64 + SDL3 NDK 入口），这是最长的一仗；iOS 同理（需 Metal/GLES 渲染验证 + UIKit 生命周期）。
5. **中文扫尾**：`Data/Local` bmd 数据文本（物品/技能名）与 PlugInResources 中文；繁简术语人工校对。
6. **签名**：macOS 公证与 Windows 代码签名（你已说后面再处理）。
7. **上手实测**：下载三平台包真机跑一遍（建号→进游戏→手柄→改帧率→切中文），把体验问题反馈回来，我按图修复。

---

### 附：证据索引
- CI 失败日志：`openmu-solo-build` runs 33960424433（linux: `Missing game asset: Data/Local/Mix.bmd`；osx: `InvalidDataException` 控制通道进程校验）
- macOS 修复对比：快照 `Path.GetFullPath` vs 仓库3 `LocalPlatform.ResolveExecutablePath`（OpenMuServerManager.cs:343）
- 素材一致性：`Glace678/3` 与 `sven-n/MuMain@a410f3de` 的 `src/bin/Data` git tree sha 均为 `9c931aa475189187`
- 帧率审计：`MuMain/docs/framerate-audit.md`（2026-09-05 static pass）
- 手柄覆盖：`MuMain/docs/controller-coverage.md` / `.csv`
- 自动登录：`MuMain/docs/local-login.md`、`OpenMU/src/LocalLauncher/LocalStackSettings.cs`（`AutomaticGameLogin = true`）
